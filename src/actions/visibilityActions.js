const showCompleted = () => ({ type: "SHOW_COMPLETED" })

const showActive = () => ({ type: "SHOW_ACTIVE" })

const showAll = () => ({ type: "SHOW_ALL" })

export { showCompleted, showActive, showAll }
